Generate a new educational exercise for Project Gem.

Arguments: $ARGUMENTS

Use the existing coordinator (or generate + validate tools) and return a clean JSON exercise.
Prefer age-appropriate content for the stated grade.